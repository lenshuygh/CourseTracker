user
----
id
username
password

create table user (
	id int not NULL,
	username varchar(25) not null,
	password varchar (25) not null,
	primary key (id));


courseEntry
------------
id
userId (user:id)
courseId(course:id)
notes
tags
completed


create table courseentry(
	id int not null,
	userid int not null,
	courseid int not null,
	notes varchar(500),
	tags varchar(200),
	completed boolean not null,
	primary key(id),
	foreign key(userid) references user(id),
	foreign key(courseid) references course(id));

course
------
id
title
author
url
subjects
publishDate
tags

create table course(
	id int not null,
	title varchar(50) not null,
	author varchar (30),
	url varchar (150) not null,
	subjects varchar(200),
	publishdate date,
	tags varchar (100),
	primary key(id));



docker mysql w docker data volume container:

  data volume container ->

    docker create -v /var/lib/mysql --name mysqldata mysql:5.7.22

  mysql server container ->

    docker run --name mysqldb --volumes-from mysqldata -e MYSQL_ROOT_PASSWORD=tracker -p 3306:3306 mysql:5.7.22

  data is persisted in the mysqldata image, after mysqldb is stopped the data is saved for next use of mysql db ->

    docker start mysqldb
